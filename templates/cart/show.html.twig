{% extends 'base.html.twig' %}

{% block body %}
    <div class="max-w-7xl mx-auto px-8 flex mt-12">
        <main class="w-2/3 pr-8">
            <h1 class="text-lg font-bold text-nfblue">
                Your cart
            </h1>
            {{ include('cart/_table.html.twig') }}
        </main>

        <aside class="w-1/3 mt-12 flex flex-col">
            {{ include('cart/_form.html.twig') }}
        </aside>
    </div>
{% endblock %}
